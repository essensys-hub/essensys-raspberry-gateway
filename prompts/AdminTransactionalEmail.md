# Prompt : emails transactionnels admin — portail utilisateur (`mon.essensys.fr`)

Voir la version détaillée : `../../prompts/AdminTransactionalEmail.md`

OpenSpec : `openspec/changes/essensys-admin-transactional-email/`

## Résumé agent

Implémenter emails transactionnels (templates admin, resend, auto-send create/allocation) dans `essensys-user-portal-backend` + UI `essensys-support-site/site`. SMTP via `vault_smtp_*` existant. Suivre les specs sous `openspec/changes/essensys-admin-transactional-email/specs/`.
